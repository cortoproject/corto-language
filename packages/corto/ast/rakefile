require 'pathname'

PACKAGE = 'corto/ast'

YACC = "ast.y"
LEX = "ast.l"

CFLAGS = ["-Wunused-parameter"]

lexyacc_dir = Pathname.new(__FILE__).dirname

require "#{lexyacc_dir}/lexyacc"
require "#{ENV['CORTO_BUILD']}/package"
